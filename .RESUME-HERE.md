# EchoTube — Resume Here

**Last touched:** 2026-06-14, before 1pm work shift
**Status:** HTML ✓ CSS ✓ — JS needs debug session

## What's done
- `css/style.css` — fully styled, pruned, verified clean
- `index.html` — 5 small fixes landed (search input type, empty-msg typo, 4× data-view alignment)

## What needs work (in order)
Open `js/app.js` and fix these line-by-line. Logic is correct; these are all typos.

| Line | Bug | Fix |
|------|-----|-----|
| 30  | `classList.toggle('active', ...)` | add `link.` prefix |
| 60  | `&order=${order}` | change to `${filter}` |
| 69  | `${data.items.length 1 ? '' : 's'}` | add `===` |
| 73  | `$searchPageResults` | change to `$searchResults` |
| 78  | `consol.error` | add `e` → `console.error` |
| 85  | `const completeSearch(event){` | add `= ` after `)` |
| 95  | `item.id.videoId` | change to `newItem.id.videoId` |
| 113 | `alt="${escapeAttr(title)}"` | add `}` before `"` |
| 146 | `JSON.stringify(bookmarks)` | change to `JSON.stringify(bookmark)` |
| 148 | `return.getBookmarks()` | space after `return` |
| 181 | `$bookmarkSort` | change to `$sortBookmarks` |
| 182 | `sortValue.split((a,b)=>{...})` | split takes string, not comparator — rewrite as `[field, dir] = sortValue.split('-'); const sorted = [...bookmarks].sort((a,b) => ...)` |
| 190, 196 | `sorted.length` | change to `bookmarks.length` |
| 235 | `chip.onClick` | lowercase → `chip.onclick` |
| 260 | queries `.thumbnail` but JS creates `thumnailItem` | pick one spelling (recommend `thumbnail` in template) |
| 272, 35-36 | calls `renderBookmarksView()` / `renderHomeView()` | actual names: `renderBookmarkSection` / `renderHomeSection` — rename call sites |
| 287 | `const escapeAttr(str){` | add `= ` after `)` |

## After JS parses
1. Open `index.html` in browser, check DevTools console
2. Click through all 4 nav sections — verify show/hide works
3. Type a search — verify it hits the API (server.js may need starting)
4. Bookmark something, refresh page, verify it persists

## Reference
- `~/Projects/EchoVideoBAKup/` — original working project (reference for behavior)
- `~/Projects/EchoTube/css/style.css` — finished stylesheet
